<header class="sticky top-0 z-30 h-16 w-full bg-base-100 bg-opacity-80 backdrop-blur border-b border-white/5 shadow">
  <div class="navbar md:px-10 h-full">
    <div class="navbar-start">
      {# Burger menu #}
      <label for="drawer" tabindex="0" role="button" class="btn btn-ghost drawer-button lg:hidden">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h8m-8 6h16" /></svg>
      </label>
      {# Logo #}
      <a class="btn btn-ghost px-2" href="/">
        <span class="sr-only">Home</span>
        <img
          class="w-10"
          src="{{ site.images.brick.src }}"
          alt="{{ site.images.brick.alt }}"
        />
        <p class="hidden lg:inline font-semibold text-xl">{{ site.society.name }}</p>
      </a>
      {# Navigation #}
      <nav class="hidden lg:flex" aria-label="Global">
        <ul class="menu menu-horizontal px-1">
          {% for entry in site.navigation.global %}
          {% if entry.submenu %}
          <li>
            <details x-data x-on:click.outside="$el.removeAttribute('open')" x-on:keydown.escape.window="$el.removeAttribute('open')">
              <summary>{{ entry.text }}</summary>
              <ul class="z-30 w-56 bg-base-200 rounded-box border border-white/5 shadow-2xl">
                {% if entry.link %}<li><a href="{{ entry.link }}">Overview</a></li>{% endif %}
                {% for sub in entry.submenu %}
                <li><a href="{{ sub.link }}">{{ sub.text }}</a></li>
                {% endfor %}
              </ul>
            </details>
          </li>
          {% else %}
          <li><a href="{{ entry.link }}">{{ entry.text }}</a></li>
          {% endif %}
          {% endfor %}
        </ul>
      </nav>
    </div>
    <div class="navbar-end gap-2">
      {# Search #}
      <is-land>
        <template data-island="replace">
          <button class="btn btn-ghost px-0 sm:px-2 lg:px-4" aria-label="Search Pages" onclick="searchModal.showModal()">
            <svg class="stroke-current text-base-content" width="20" height="20" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
          </button>
        </template>
      </is-land>
      {# Theme #}
      <is-land>
        <template data-island="replace">
          {% include "global/theme-picker.njk" %}
        </template>
      </is-land>
      <a class="btn btn-primary" href="{{ site.navigation.buttons.primary.link }}" rel="noreferrer" target="_blank">
        {{ site.navigation.buttons.primary.text }}
      </a>
      <a class="btn btn-neutral" href="{{ site.navigation.buttons.secondary.link }}" rel="noreferrer" target="_blank">
        {{ site.navigation.buttons.secondary.text }}
      </a>
    </div>
  </div>
</header>
