---
layout: layout/base.njk
pagination:
  data: governance
  size: 1
  alias: document
permalink: /{{ document.url }}/
eleventyComputed:
  title: "{{ document.title }}"
---
{% from "components/sidebar.njk" import sidebar %}

<div class="md:drawer-open grid grid-cols-4 gap-4 bg-base-100 min-h-screen">
  <aside class="sticky hidden lg:block left-0 top-16 h-screen p-5 overflow-auto bg-base-100 border-r border-white/5">
    <nav class="menu">
      {{ sidebar(header="Open Governance", data=governance, current=page.url) }}
    </nav>
  </aside>
  <main class="col-span-4 md:col-span-3 lg:col-span-2">
    <article class="p-10 pb-20">
      <div class="prose min-w-full">
        {{ document.content | safe }}
      </div>
    </article>
  </main>
  <aside class="sticky hidden md:block right-0 top-16 h-screen p-5 overflow-auto bg-base-100">
    <nav class="menu">
      <li>
        <header class="menu-title">In this document</header>
        <ul>
          {{ document.content | toc | safe }}
        </ul>
      </li>
    </nav>
  </aside>
</div>